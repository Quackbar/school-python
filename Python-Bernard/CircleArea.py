#CircleArea
#Bernard Kintzing
#9/15/2016

import math

pi = math.pi
radius = input('Enter the radius of the circle: ')
radius = float(radius)
area = (radius**2*pi)
print('The area of a circle with the radius of ' + str(radius) + ' is ' + str(area))
